# Git Identity

Always use the following Git identity for commits, tags, and PRs in this repository:
- **Git user.name**: `vir92`
- **Git user.email**: `177326854+vir92@users.noreply.github.com`

Ensure git local config is set with:
`git config user.name "vir92"`
`git config user.email "177326854+vir92@users.noreply.github.com"`
